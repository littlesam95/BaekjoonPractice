#include <iostream>

using namespace std;

int main() {
	cin.tie(NULL);
	ios_base::sync_with_stdio(false);

	string str;
	int A, B;
	int count = 0;
	while (cin >> A >> B) {
		cout << A + B << endl;
	}

	getchar();
	getchar();
	return 0;
}