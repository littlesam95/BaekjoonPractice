#include <iostream>
#include <string>
#define FIRST cin.tie(NULL); cout.tie(NULL); ios::sync_with_stdio(false);

using namespace std;
string ID;

int main() {
	FIRST
	cin >> ID;
	cout << ID + "??!" << "\n";

	getchar();
	getchar();
	return 0;
}